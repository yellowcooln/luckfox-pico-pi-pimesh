# Replace the placeholder values below before enabling the Wi-Fi client.
ctrl_interface=/var/run/wpa_supplicant
update_config=1
country=US

network={
  ssid="YOUR_WIFI_SSID"
  psk="YOUR_WIFI_PASSWORD"
  key_mgmt=WPA-PSK
}
